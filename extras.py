                # def cb():
                #     pred = n.predict(X)
                #     plt.plot(X, pred)
                #     plt.plot(rbfs_mean, [0] * len(rbfs_mean), "ro")
                #     plt.plot(X, Y)
                #     plt.title(f"{hidden_num}, {rbfs_variance}")
                #     plt.show(block=False)
                #     plt.pause(0.001)
                #     plt.clf()